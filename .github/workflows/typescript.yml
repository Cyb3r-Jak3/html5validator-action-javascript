name: 'Typescript Test'
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'
    paths:
      - "**.ts"
      - ".github/workflows/typescript.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v2
        with:
          node-version: '12'
          cache: 'yarn'

      - name: Install Packages
        run: yarn install

      - name: Lint
        run: yarn run lint

      - name: Check format
        run: yarn run format-check

      # - name: Run tests
      #   run: yarn run test

      # If this fails then run `yarn build` and push new dist
      - name: Check Build
        run: |
          sudo apt-get install dos2unix
          yarn run build-check
          dos2unix -f dist/index.js check/index.js
          cmp dist/index.js check/index.js